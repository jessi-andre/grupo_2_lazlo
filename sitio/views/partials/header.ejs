<% if (locals.loginUsuario && locals.loginUsuario.category=="admin" ) { %>
  <header class="headerAdmin">
    <nav class="navAdmin_productos">
      <a href="/productos/administrador"><i class="fas fa-user-cog"></i> Administrador</a>
      <a href="/productos/agregar-productos"><i class="fas fa-plus"></i> Agregar productos</a>
      </ul>
    </nav>

    <nav class="navAdmin">
      <a href="/users/perfilAdmin" class="navAdmin_btn">
        <p>Hola, <%= locals.loginUsuario.first_name %>
        </p> <img src="/images/users/<%= locals.loginUsuario.image %>" alt="">
      </a>
    </nav>
  </header>
  <% } else {%>
    <span></span>
    <% } %>

      <header>
        <a href="/" class="logo">
          <img src="/images/logo.png" alt="" />
        </a>
        <aside class="headerNav">
          <div class="btn-cerrarMenu">
            <i class="fas fa-times"></i>
          </div>
          <nav>
            <ul>
              <li><a href="/productos/categoria/mochilas">mochilas</a></li>
              <li><a href="/productos/categoria/indumentaria">indumentaria</a></li>
              <li><a href="/historia">historia</a></li>



              <li><a href="/comunidad">comunidad</a></li>

              <form action="/search" class="busqueda">
                <input type="search" name="busqueda" id="busqueda" placeholder="Busca tu producto..." />
                <button><i class="fas fa-search"></i></button>

              </form>
            </ul>
          </nav>
        </aside>
        <div class="btns-menu">
          <% if (locals.loginUsuario && locals.loginUsuario.category==='usuario' ){ %>
            <div class="btn-profile">
              <a href="/users/perfil">
                <div class="avatar-usuario">
                  <img src="/images/users/<%= locals.loginUsuario.image %> " alt="" />
                  <h2>
                    <%= locals.loginUsuario.first_name %>
                  </h2>
                </div>
              </a>
            </div>
            <% }else { %>
              <div class="btn-user">
                <a href="/users/login"><i class="fas fa-user"></i></a>
              </div>

              <% } %>
                <div class="btn-carrito">
                  <a href="/carrito"><i class="fas fa-shopping-bag"></i></a>
                </div>
                <div class="btn-menu">
                  <i class="fas fa-bars"></i>
                </div>
        </div>
      </header>